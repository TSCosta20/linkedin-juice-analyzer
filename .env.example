# URL of the scoring proxy
VITE_PROXY_URL=https://tiagoscosta.com/api/score

# Shared secret between extension and proxy — must match PROXY_SECRET on the server
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_PROXY_SECRET=your_proxy_secret_here
